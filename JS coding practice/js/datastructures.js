/*------------------------------------------------------------------------------------------------------ */
class Stack {
    enqueue = () => {}
    dequeue = () => {}
}

// create an object of class and use switch case. Pass the array as a parameter to the class contructor
// create a class instead of a structure in javascript data structure 